# UI 框架架构

## 设计目标

- 单场景架构：所有界面均为预制体动态挂载，不切换场景
- 分层管理：Canvas 下按视觉与交互层级划分为五个固定父节点
- 管理器职责分离：界面(UIManager)、弹窗(PopupManager)、提示(TipManager)各自独立
- 配置表驱动：所有 UI 类型信息由外部 JSON 配置表定义
- 防呆机制：通过配置表注册、启动自检

---

## 目录结构

```
assets/
├── Script/
│   ├── base/                   # 基类
│   │   ├── BaseUI.ts           # 通用基类（继承 cc.Component）
│   │   ├── BasePage.ts         # 页面基类（继承 BaseUI）
│   │   └── BasePopup.ts        # 弹窗基类（继承 BaseUI）
│   ├── manager/                # 管理器（单例模式）
│   │   ├── UIManager.ts        # 界面管理器
│   │   ├── PopupManager.ts     # 弹窗管理器
│   │   └── TipManager.ts       # 提示管理器
│   ├── utils/
│   │   └── common.ts           # 通用工具函数
│   └── Main.ts                 # 主场景入口
├── resources/
│   └── config/                 # 配置表
│       ├── panel_config.json   # 界面配置
│       ├── popup_config.json   # 弹窗配置
│       └── tip_config.json     # 提示配置
└── Scene/
    └── mian.fire               # 主场景（五层节点）
```

---

## 核心设计

### 1. 五层 Canvas 结构

| 节点名称 | 用途 | 管理者 |
|----------|------|--------|
| veryBottomLayer | 极底层（背景、粒子底板） | - |
| bottomLayer | 底层（静态装饰元素） | - |
| viewLayer | 视图层（普通功能界面） | UIManager |
| popupLayer | 弹窗层（模态弹窗、对话框） | PopupManager |
| tipLayer | 系统提示层（Toast、网络提示） | TipManager |

### 2. 继承链

```
cc.Component
    ↓
  BaseUI (注入 UIManager/PopupManager/TipManager/utils)
    ↓
  BasePage (页面生命周期：onShow/onClose/onNavigate)
  BasePopup (弹窗生命周期：open/close/beforeClose)
    ↓
  具体界面/弹窗组件
```

### 3. 单例模式

所有管理器使用 getInstance() 写法：

```typescript
class UIManagerClass {
    private static _instance: UIManagerClass | null = null;

    static getInstance(): UIManagerClass {
        if (!this._instance) {
            this._instance = new UIManagerClass();
        }
        return this._instance;
    }
}
export default UIManagerClass;
```

### 4. 懒加载层节点

管理器通过懒加载方式获取层节点，避免场景未加载完成时获取失败：

```typescript
private ensureLayer(): cc.Node | null {
    if (!this.viewLayer) {  
        const canvas = cc.director.getScene()?.getChildByName('Canvas');
        this.viewLayer = canvas?.getChildByName('viewLayer') || null;
    }
    return this.viewLayer;
}
```

---

## 配置表格式

### panel_config.json（界面）

```json
{
    "ShopPanel": {
        "path": "prefabs/shop/ShopPanel"
    }
}
```

### popup_config.json（弹窗）

```json
{
    "ConfirmDialog": {
        "path": "prefabs/common/ConfirmDialog",
    }
}
```

### tip_config.json（提示）

```json
{
    "ToastTip": {
        "path": "prefabs/common/ToastTip",
        "duration": 1.5
    }
}
```

---

## 使用示例

### 1. 挂载 Main 组件

在主场景 mian.fire 的 Canvas 节点上挂载 Main.ts

### 2. 界面开发

```typescript
// 继承 BaseUI 的界面组件
var ShopPanel = cc.Class({
    extends: BaseUI,

    onShow: function(data) {
        // 界面管理
        this.UIManager.show('OtherPanel', { id: 1 });
        this.UIManager.hide('ShopPanel');

        // 弹窗管理
        this.PopupManager.show('ConfirmDialog', { text: '确认？' });

        // 提示管理
        this.TipManager.showTip('ToastTip', { text: '操作成功' });
    }
});
```

---

## 启动流程

1. 场景加载 → Canvas 的 Main 组件 onLoad
2. BaseUI 构造时调用 getInstance() 获取管理器实例
3. 首次调用管理器方法时，懒加载获取层节点
4. 配置表分帧异步加载

---

## 更新日志

- 初始版本：五层 Canvas + 三个管理器 + Base 类继承链
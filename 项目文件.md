# Gobang（五子棋）项目文件

## 项目概述

| 项目信息 | 内容 |
|---------|------|
| 项目类型 | Cocos Creator 2.4.3 游戏项目 |
| 游戏类型 | 五子棋（Gobang/Five-in-a-Row） |
| 开发语言 | TypeScript（新 UI 框架）/ JavaScript（游戏逻辑） |
| 模块系统 | ES6 import/export（新）/ 无模块（旧） |
| 引擎 | Cocos2d-html5 |
| 目标平台 | 移动端网页 |
| 主要功能 | PvP 对战、PvE 对战（AI）、游戏回放、音效管理、设置管理、单场景 UI 框架 |

## 目录结构

```
gobang/
├── assets/
│   ├── Script/                  # TypeScript 游戏逻辑（新 UI 框架）
│   │   ├── base/                # 基类
│   │   │   ├── BaseUI.ts         # 通用基类
│   │   │   ├── BasePage.ts       # 页面基类
│   │   │   └── BasePopup.ts     # 弹窗基类
│   │   ├── manager/             # 管理器
│   │   │   ├── UIManager.ts     # 界面管理器
│   │   │   ├── PopupManager.ts  # 弹窗管理器
│   │   │   ├── TipManager.ts    # 提示管理器
│   │   │   ├── AudioManager.ts  # 音效管理
│   │   │   ├── ReplayManager.ts # 回放管理
│   │   │   ├── SettingManager.ts # 设置管理
│   │   │   ├── ResourceManager.ts # 资源管理
│   │   │   ├── EventEmitter.ts  # 事件发射器
│   │   │   └── TimeManager.ts   # 时间管理
│   │   ├── config/              # 配置表
│   │   ├── utils/               # 工具函数
│   │   │   ├── common.ts        # 通用工具函数
│   │   │   ├── MathUtils.ts    # 向量与数学工具（向量运算、角度、距离、碰撞、插值、随机）
│   │   │   ├── Queue.ts        # 队列（FIFO）
│   │   │   ├── Stack.ts         # 栈（LIFO）
│   │   │   ├── LinkedList.ts   # 双向链表（含迭代器）
│   │   │   └── Storage.ts       # 本地存储（统一加密存/取）
│   │   ├── Main.ts             # 主场景入口
│   │   ├── LoginLayer.ts       # 登录界面
│   │   └── SettingLayer.ts     # 设置界面
│   ├── Scene/                   # .fire 场景文件
│   ├── Prefab/                  # 可复用预制体
│   ├── Texture/                 # 图片资源
│   └── resources/               # 音频等资源
├── settings/                    # 项目设置
├── project.json                 # 项目配置
├── package.json                 # Node 依赖配置
├── tsconfig.json                # TypeScript 配置
└── jsconfig.json               # JavaScript 配置
```

## 架构设计

### 单场景架构

项目采用单场景架构，所有界面元素通过预制体动态挂载，不切换场景。

### 五层 Canvas 结构

| 节点名称 | 用途 | 管理者 |
|----------|------|--------|
| veryBottomLayer | 极底层（背景、粒子底板） | - |
| bottomLayer | 底层（静态装饰元素） | - |
| viewLayer | 视图层（普通功能界面） | UIManager |
| popupLayer | 弹窗层（模态弹窗、对话框） | PopupManager |
| tipLayer | 系统提示层（Toast、网络提示） | TipManager |

### 单例模式

所有管理器使用 getInstance() 单例写法：

```typescript
class UIManagerClass {
    private static _instance: UIManagerClass | null = null;

    static getInstance(): UIManagerClass {
        if (!this._instance) {
            this._instance = new UIManagerClass();
        }
        return this._instance;
    }
}
export default UIManagerClass;
```

### 懒加载层节点

管理器通过懒加载方式获取层节点：

```typescript
private ensureLayer(): cc.Node | null {
    if (!this.viewLayer) {
        const canvas = cc.director.getScene()?.getChildByName('Canvas');
        this.viewLayer = canvas?.getChildByName('viewLayer') || null;
    }
    return this.viewLayer;
}
```

### 继承链

```
cc.Component
    ↓
  BaseUI (注入 UIManager/PopupManager/TipManager/utils)
    ↓
  BasePage (页面生命周期：onShow/onClose/onNavigate)
  BasePopup (弹窗生命周期：open/close/beforeClose)
    ↓
  具体界面/弹窗组件
```

## 场景文件说明

### 场景列表

| 场景文件 | 路径 | 说明 |
|---------|------|------|
| **mian.fire** | `assets/Scene/` | 主场景（五层 Canvas 结构） |
| **Login.fire** | `assets/Scene/` | 主菜单场景（旧，提供游戏入口） |
| **Battle.fire** | `assets/Scene/` | 对战场景，核心游戏界面 |
| **Setting.fire** | `assets/Scene/` | 设置场景（旧，音量和难度配置） |

### 场景切换关系

```
mian.fire（主场景 - 五层 Canvas）
    ↓ 通过 UIManager 动态挂载
├── LoginLayer → Battle场景
└── SettingLayer

（旧架构）
Login.fire ──→ Battle.fire
     │              │
     │              ↓
     └──→ Setting.fire
```

## 脚本文件说明

### UI 框架脚本（TypeScript）

#### base/ 基类

| 脚本文件 | 说明 |
|----------|------|
| **BaseUI.ts** | 通用基类，继承 cc.Component，注入管理器 |
| **BasePage.ts** | 页面基类，继承 BaseUI，新增生命周期 |
| **BasePopup.ts** | 弹窗基类，继承 BaseUI，新增生命周期 |

#### manager/ 管理器

| 脚本文件 | 说明 |
|----------|------|
| **UIManager.ts** | 界面管理器：show/hide/push/pop |
| **PopupManager.ts** | 弹窗管理器：open/close/setMask |
| **TipManager.ts** | 提示管理器：showTip |
| **AudioManager.ts** | 音效管理：背景音乐、音效加载、音量控制 |
| **ReplayManager.ts** | 回放控制：开始/暂停/停止/跳转 |
| **SettingManager.ts** | 设置持久化：localStorage 读写 |
| **ResourceManager.ts** | 资源管理：资源加载、注册、释放 |
| **EventEmitter.ts** | 事件发射器：事件监听与分发 |
| **TimeManager.ts** | 时间管理：游戏内计时器 |

#### utils/ 工具函数

| 脚本文件 | 说明 |
|----------|------|
| **common.ts** | 通用工具函数：延迟、克隆、随机、格式化等 |
| **MathUtils.ts** | 向量与数学工具：Vector2类、向量运算、角度转换、距离计算、碰撞检测、插值、随机 |
| **Queue.ts** | 队列（FIFO）：enqueue/dequeue/front/back/size/toArray/forEach |
| **Stack.ts** | 栈（LIFO）：push/pop/peek/size/toArray/forEach/peekMultiple |
| **LinkedList.ts** | 双向链表：append/insertAt/remove/get/find/toArray/forEach/iterator |

#### 界面脚本

| 脚本文件 | 说明 |
|----------|------|
| **Main.ts** | 主场景入口，挂载在 Canvas 节点上 |
| **LoginLayer.ts** | 登录/主菜单界面 |
| **SettingLayer.ts** | 设置界面 |

#### config/ 配置表

| 脚本文件 | 说明 |
|----------|------|
| **panel_config.json** | 界面配置：path、prefab 路径 |
| **popup_config.json** | 弹窗配置：path、maskClose、priority |
| **tip_config.json** | 提示配置：path、duration |

### 游戏逻辑脚本（JavaScript）

#### 核心脚本

| 脚本文件 | 说明 |
|----------|------|
| **001_GameConfig.js** | 全局配置对象 |
| **Battle.js** | 核心对战逻辑 |
| **Chess.js** | 棋子 UI 与行为 |
| **AI.js** | AI 算法实现 |

#### UI 与管理脚本

| 脚本文件 | 说明 |
|----------|------|
| **Login.js** | 主菜单逻辑（旧场景） |
| **WinPopupManager.js** | 胜利弹窗管理 |
| **GameInfoManager.js** | 游戏信息面板 |
| **ReplayUI.js** | 回放控制 UI |

#### 系统脚本

| 脚本文件 | 说明 |
|----------|------|
| **AudioManager.js** | 音效管理（旧） |
| **SettingManager.js** | 设置持久化（旧） |
| **SettingScene.js** | 设置场景逻辑 |
| **ReplayManager.js** | 回放控制核心 |
| **ParticleManager.js** | 粒子系统管理 |

## 配置表格式

### panel_config.json（界面）

```json
{
    "ShopPanel": {
        "path": "prefabs/shop/ShopPanel"
    }
}
```

### popup_config.json（弹窗）

```json
{
    "ConfirmDialog": {
        "path": "prefabs/common/ConfirmDialog",
        "maskClose": true,
        "priority": 0
    }
}
```

### tip_config.json（提示）

```json
{
    "ToastTip": {
        "path": "prefabs/common/ToastTip",
        "duration": 1.5
    }
}
```

## 预制体说明

### Chess.prefab
- **路径**: `assets/Prefab/Chess.prefab`
- **作用**: 可复用的棋子组件模板
- **包含组件**:
  - Chess.js 脚本
  - Sprite 组件
  - sptFrames（Chess_0.png, Chess_1.png）
- **使用方式**: 通过 `pfchess` 引用实例化

### 依赖关系
```
Chess.prefab
├── Chess.js（棋子逻辑）
├── Sprite（显示组件）
└── sptFrames
    ├── Chess_0.png（黑子）
    └── Chess_1.png（白子）
```

## 资源文件说明

### Texture 目录结构
```
assets/Texture/
├── BattleBG.png        # 对战背景
├── MapBG.png           # 棋盘背景
├── LoginBG.png         # 登录背景
├── SettingBG.png       # 设置背景
├── Logo.png            # 游戏 Logo
├── Chess_0.png         # 黑子图片
├── Chess_1.png         # 白子图片
├── Btn.png             # 按钮基础
├── Btn_Start.png       # 开始按钮
├── Btn_Setting.png     # 设置按钮
├── Btn_Return.png      # 返回按钮
├── Btn_Undo.png        # 悔棋按钮
├── Slider_BG.png       # 滑块背景
├── Slider_Handle.png   # 滑块手柄
├── Info_Panel_BG.png   # 信息面板背景
├── icon_exit.png       # 退出图标
├── icon_pause.png      # 暂停图标
├── icon_play.png       # 播放图标
├── icon_speed.png      # 速度图标
├── icon_step_back.png  # 后退图标
├── icon_step_forward.png # 前进图标
└── sureBtnBg.png       # 确认按钮背景
```

### resources 目录
```
assets/resources/
├── Font/
│   ├── font.fnt        # 字体描述文件
│   └── font.png        # 字体纹理
└── Audio/
    ├── bgm_battle.wav  # 对战背景音乐
    ├── bgm_menu.wav    # 菜单背景音乐
    ├── buttonClick.wav # 按钮点击音效
    ├── menuClick.wav   # 菜单点击音效
    ├── placeChess.wav  # 落子音效
    ├── preview.wav     # 预览音效
    └── win.wav         # 胜利音效
```

## 文件依赖关系

### UI 框架依赖图
```
Main.ts
    ↓
UIManager.ts（动态加载 LoginLayer）
    ↓
LoginLayer.ts（通过配置表）
    ↓ (切换到 Battle)
Battle.fire + Battle.js
```

### 游戏核心依赖图
```
001_GameConfig.js（全局配置）
       ↓
    Battle.js（核心逻辑）
    ↓       ↓
Chess.prefab  AI.js
    ↓           ↓
Chess.js      Minimax
    ↓
Sprite + sptFrames
```

### 系统依赖关系
```
Battle.js
├── AudioManager（音效）
├── GameInfoManager（信息面板）
├── WinPopupManager（胜利弹窗）
├── ReplayManager（回放控制）
├── ReplayUI（回放 UI）
└── SettingManager（设置读取）
       ↓
   localStorage
```

### 关键依赖链
1. **落子流程**: Battle.js → Chess.prefab → Chess.js → Sprite
2. **AI 流程**: Battle.js → AI.js → GameConfig.AI → 返回落子位置
3. **音效流程**: Battle.js → AudioManager → resources/Audio/
4. **设置流程**: SettingScene.js → SettingManager → localStorage
5. **回放流程**: Battle.js → ReplayManager → ReplayUI

## 代码风格规范

### 组件模式（TypeScript + 装饰器）

```typescript
import BaseUI from './base/BaseUI';

const { ccclass } = cc._decorator;

@ccclass
export default class LoginLayer extends BaseUI {
    // 编辑器暴露的属性，带类型注释
    private prefabName: cc.Prefab = null;
    private nodeName: cc.Node = null;
    private labelName: cc.Label = null;
    private valueName: 0 | 1 = 0;
    private textName: "" | "default" = "";
    private flagName: boolean = false;
    private spriteFrames: cc.SpriteFrame[] = [];

    onLoad() { /* 初始化 */ },
    start() { /* 首次激活逻辑 */ },
    onDestroy() { /* 清理 */ },
}
```

### 模块系统（ES6 import/export）

```typescript
// 导出
import BaseUI from './base/BaseUI';

// 导入时不能加 .ts 后缀
```

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件类 | PascalCase | `WinPopupManager` |
| 文件名（TS） | PascalCase | `Battle.ts`, `Chess.ts` |
| 文件名（JS） | PascalCase | `Battle.js`, `Chess.js` |
| 属性 | camelCase | `chessWidth`, `mapHeight` |
| 私有方法 | 下划线前缀 | `_iswin()`, `_getChessPosition()` |
| 事件处理 | on 前缀 | `onRestartClick()`, `onReturn()` |
| 布尔标志 | is/has 前缀 | `isPopupShowing`, `gameOver` |
| 节点引用 | nd 前缀 | `ndBase`, `ndChess` |
| 精灵帧 | spt 前缀 | `sptFrames` |

## 重要说明

### 开发规范与禁忌

#### 绝对禁止的操作
1. **绝对不能重命名已有文件** — 会导致编辑器绑定丢失、脚本组件引用失效
2. **绝对不能批量修改多个文件** — 容易遗漏和出错
3. **绝对不能删除已有文件** — 可能破坏依赖关系

#### 修改前必须确认
1. 修改代码前必须先说明要做什么
2. 说明可能的影响
3. 等用户确认后再执行

#### 预制体修改规范
1. 不能直接修改预制体，必须先确认方案
2. 不能给子节点添加 PrefabInfo（PrefabInfo 只能有一个，指向根节点）
3. 不能修改根节点的组件引用
4. 先读取当前结构，分析现有代码依赖

### 关键技术要点
1. **游戏逻辑**: JavaScript（无模块系统）
2. **UI 框架**: TypeScript + ES6 import/export
3. **中文注释**: 整个代码库使用中文注释
4. **游戏逻辑**: `count >= 4` 表示五子连珠（count 不包含当前落子）
5. **对象池**: 棋子使用对象池管理，减少 GC 压力
6. **本地存储**: 设置通过 localStorage 持久化

### 性能优化策略
1. **对象池复用**: 棋子节点预创建，回收复用
2. **异步 AI 计算**: 分批处理落子评分，避免主线程阻塞
3. **动画模板复用**: Chess 组件中动画对象只创建一次
4. **标记节点复用**: 最后落子标记节点只创建一次，通过 active 切换
5. **懒加载层节点**: 管理器首次访问时再获取层节点

## 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| **cocos注意事项.md** | 项目根目录 | Cocos 废弃接口、版本限制、常见坑点 |
| **开发规范与错误���录.md** | 项目根目录 | 开发规范、错误教训、操作检查清单 |
| **AGENTS.md** | 项目根目录 | AI 协作规则和代码风格指南 |
| **Git提交操作指南.md** | 项目根目录 | Git 操作指南和常用命令 |

## 构建与发布

### 本地构建
```bash
# 通过 Cocos Creator 编辑器 GUI 构建：
# 项目 -> 构建 -> 选择平台 -> 构建
# 无命令行构建方式
```

### GitHub Pages 发布
```bash
# 每次发布只需执行以下命令：
cd build/web-mobile
rm -rf .git  # 清理旧的 git 目录（如果有）
git init
git add .
git commit -m "发布版本-$(date +%Y%m%d)"
git push -f https://github.com/qzyingfeng/gobang.git master:gh-pages
```

**注意**: 不要使用 `master:gh-pages`，会把整个项目推上去，GitHub 会当作 Jekyll 站点处理